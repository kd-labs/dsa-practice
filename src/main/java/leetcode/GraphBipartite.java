package leetcode;

public class GraphBipartite {

    /*
        A Bipartite graph is one which either does not has a cycle in it
        and if cycle is present then it does not contain an odd length cycle
     */
    public boolean isBiPartite(int[][] graph) {

        return false;
    }
}
